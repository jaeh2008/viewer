services:
  - type: web
    name: eduplay
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: ADMIN_PASSWORD
        value: 여기에_비밀번호_설정   # ← 반드시 변경하세요
